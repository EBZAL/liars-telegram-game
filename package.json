{
  "name": "liars-telegram-game",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspace=@liars-telegram-game/game-core",
    "test": "npm run test --workspace=@liars-telegram-game/game-core"
  }
}
