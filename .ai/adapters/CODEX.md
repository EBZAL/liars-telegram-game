# Codex Adapter

Codex normally runs in `IMPLEMENTATION_MODE`.

Use the Implementation Executor path in `AGENTS.md`.

Read:
- `.ai/EXECUTOR_RUNTIME.md`
- current Task Context Capsule
- MUST_READ files/tests

Expand context only for a concrete coding reason.

When authorized:
- persist exact approved task;
- READY → IN_PROGRESS;
- task-start state commit;
- implementation commit;
- concise Evidence;
- Ledger → IMPLEMENTED;
- Evidence/state commit;
- push.

Never decide VERIFIED.

Gravity remains the default State Sync writer unless explicitly changed by Architect.
